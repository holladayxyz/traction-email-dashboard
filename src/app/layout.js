export const metadata = {
  title: "XYZ Strategy — Email Traction Dashboard",
  description: "Email traction dashboard by XYZ Strategy.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, padding: 0 }}>{children}</body>
    </html>
  );
}
